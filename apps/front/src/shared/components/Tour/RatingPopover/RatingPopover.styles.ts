import { Box } from '@mui/material'
import styled from 'styled-components'


const Wrapper = styled(Box)({
    display: 'flex',
    alignItems: 'center',
    marginRight: 6
})

export default {
    Wrapper
}