import styled from 'styled-components'

const Content = styled.main(
    {
        padding: 18,
        height: 'calc(100vh - 55px)',
        display: 'flex',
        flexFlow: 'column',
        width: '100%',
        marginTop: 55
    }
)

export default {
    Content
}